{
  "name": "cli-run-ui",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "local": "pnpm install && pnpm dev",
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.6.3"
  }
}
